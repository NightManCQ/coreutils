// This file is part of the uutils coreutils package.
//
// For the full copyright and license information, please view the LICENSE
// file that was distributed with this source code.

使用 箱::错误::UError;

/// Error types used by buffer-copying functions from the `buf_copy` module.
#[derive(调试协议)]
公有 枚举 错误协议 {
    Io(标准库::输入输出::错误协议),
    WriteError(字符串),
}

实现 标准库::格式化::显示协议 循环 错误协议 {
    方法 格式化(&自身, f: &可变 标准库::格式化::Formatter<'_>) -> 标准库::格式化::结果 {
        匹配 自身 {
            自身类型::WriteError(msg) => write!(f, "splice() write error: {msg}"),
            自身类型::Io(err) => write!(f, "I/O error: {err}"),
        }
    }
}

实现 标准库::错误::错误协议 循环 错误协议 {}

实现 UError 循环 错误协议 {
    方法 code(&自身) -> 整32 {
        1
    }

    方法 usage(&自身) -> 布尔 {
        假
    }
}
