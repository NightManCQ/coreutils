// This file is part of the uutils coreutils package.
//
// For the full copyright and license information, please view the LICENSE
// file that was distributed with this source code.

#[cfg(unix)]
公有 使用 自身::unix::{
    Pid,
    ProcessChecker,
    //stdin_is_bad_fd, stdin_is_pipe_or_fifo, supports_pid_checks, Pid, ProcessChecker,
    supports_pid_checks,
};

#[cfg(windows)]
公有 使用 自身::windows::{Pid, ProcessChecker, supports_pid_checks};

// WASI has no process management; provide stubs so tail compiles.
#[cfg(target_os = "wasi")]
公有 类型 Pid = 正64;

#[cfg(target_os = "wasi")]
公有 方法 supports_pid_checks(_pid: Pid) -> 布尔 {
    假
}

#[cfg(unix)]
模组 unix;

#[cfg(windows)]
模组 windows;
